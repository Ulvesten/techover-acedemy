const queue = [21, 52, 12, 19, 17, 16, 26];
/*
for (let i = 0; i < queue.length; i++) {
  const age = queue[i];
  if (age < 18) {
    console.log(`Du är bara ${age}, Kom tbks när du är 18`);
  } else if (age > 18) {
    console.log(`Kom in du är ${age}`);
  }
}
*/

for (let i = 0; i < queue.length; i++) {
  const age = queue[i];
  if (age < 18) {
    console.log(`Du är bara ${age}, Kom tbks när du är 18`);
  } else if (age > 18) {
    console.log(`Kom in du är ${age}`);
  }
}
